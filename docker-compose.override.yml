services:
  app:
    build:
      target: dev
    environment:
      WATCHPACK_POLLING: "true"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: sh -c "node node_modules/prisma/build/index.js migrate deploy && npm run dev"
